const fs=require('fs'),vm=require('vm');const harness=fs.readFileSync('tests/test-client-v3.cjs','utf8').split('const test=s=>')[0];const ctx={require,console,AbortController,structuredClone};vm.createContext(ctx);vm.runInContext(harness,ctx);const test=s=>vm.runInContext('vm.runInContext('+JSON.stringify(s)+',sandbox)',ctx);const rows=[];
for(const difficulty of [1,2])for(let stage=0;stage<10;stage++)rows.push(test(`assetsReady=true;selected=${stage};beginLocalRun({id:'mine-boss',stage:${stage},difficulty:${difficulty},startWeapon:'shotgun'});var seed=1;Math.random=()=>{seed=seed*16807%2147483647;return seed/2147483647};run.weapons.shotgun=5;run.skills.wealth=3;checkAwakenings();run.next=run.spawn=1e9;run.hp=run.maxHp=1e9;run.bossSpawned=true;run.chests=[];spawn(true);var boss=run.enemies[0];boss.x=240;boss.y=0;end=()=>{run.ended=true};for(let i=0;i<10800&&!run.ended;i++){const dx=boss.x-run.x,dy=boss.y-run.y,d=Math.hypot(dx,dy)||1;joy=d>95?{x:dx/d,y:dy/d}:d<70?{x:-dy/d,y:dx/d}:{x:0,y:0};tick(1/60)}({difficulty:${difficulty},stage:${stage}+1,cleared:boss.hp<=0,seconds:Math.round(run.t*10)/10,damageTaken:Math.round(1e9-run.hp),damage:Math.round(run.weaponDamage.shotgun||0)})`));console.log(JSON.stringify(rows,null,2));
