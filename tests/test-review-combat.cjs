const fs=require('fs'),vm=require('vm'),assert=require('node:assert/strict');const harness=fs.readFileSync('tests/test-client-v3.cjs','utf8').split('const test=s=>')[0];const ctx={require,console,AbortController,structuredClone};vm.createContext(ctx);vm.runInContext(harness,ctx);const test=s=>vm.runInContext('vm.runInContext('+JSON.stringify(s)+',sandbox)',ctx);
test("assetsReady=true;selected=0;beginLocalRun({id:'lightning',stage:0,difficulty:0,startWeapon:'lightning'});Math.random=()=>.5;run.chests=[];run.enemies=[{x:50,y:0,hp:10000,r:15,type:0},{x:100,y:0,hp:10000,r:15,type:0},{x:1000,y:0,hp:10000,r:15,type:0}];weaponTick(.01)");assert(Math.abs(test('10000-run.enemies[0].hp')-19*2.4)<1e-8);assert(Math.abs(test('10000-run.enemies[1].hp')-19*.9)<1e-8);assert.equal(test('run.enemies[2].hp'),10000);
const snapshots=[];for(const simple of [false,true])snapshots.push(test(`simpleEffects=${simple};beginLocalRun({id:'parity',stage:0,difficulty:0,startWeapon:'lightning'});var seed=3;Math.random=()=>{seed=seed*16807%2147483647;return seed/2147483647};run.weapons={lightning:5,flame:5,shotgun:5};run.awakened={lightning:true,flame:true,shotgun:true};run.next=run.spawn=1e9;run.hp=run.maxHp=1e9;run.chests=[];run.enemies=Array.from({length:30},(_,i)=>({x:65+i*8,y:i%2*30,hp:10000,maxHp:10000,r:18,type:0,speed:0,shot:999}));for(let i=0;i<300;i++)tick(1/60);JSON.stringify({hp:run.enemies.map(e=>e.hp),damage:run.weaponDamage,gold:run.gold,seed})`));assert.equal(snapshots[0],snapshots[1]);
for(const id of ['gauss','shotgun','laser','missile','nova','drone','flame','lightning']){test(`run.weapons['${id}']=5`);const detail=test(`weaponDetails('${id}')`);assert(!detail.includes('NaN'));assert(detail.includes('피해'));}
test('pause()');assert(test('statsHTML().includes("작전 한정")'));assert(test('statsHTML().includes("영구 자산 증식")'));
console.log('PASS: lightning first/chain/range damage, simple effects preserve simulation/RNG, and all weapon stat descriptions.');
