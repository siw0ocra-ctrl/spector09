const fs=require('fs'),vm=require('vm');const harness=fs.readFileSync('tests/test-client-v3.cjs','utf8').split('const test=s=>')[0];const ctx={require,console,AbortController,structuredClone};vm.createContext(ctx);vm.runInContext(harness,ctx);const test=s=>vm.runInContext('vm.runInContext('+JSON.stringify(s)+',sandbox)',ctx);let rows=[];
for(const id of ['gauss','shotgun','laser','missile','nova','drone','flame','lightning']){const row={id};for(const mode of ['close','moving','far'])row[mode]=test(`assetsReady=true;selected=0;beginLocalRun({id:'ttk',stage:0,difficulty:0,startWeapon:'${id}'});Math.random=()=>.5;run.weapons['${id}']=5;run.awakened['${id}']=true;run.skills.rapid=3;run.spawn=run.next=1e9;run.hp=run.maxHp=1e9;run.chests=[];run.enemies=[{x:${mode==='far'?300:90},y:0,r:45,hp:5000,maxHp:5000,type:0,speed:0,shot:1e9,angle:0}];for(let i=0;i<7200&&run.enemies.length;i++){${mode==='moving'?'run.enemies[0].x=90*Math.cos(run.t*.8);run.enemies[0].y=90*Math.sin(run.t*.8);':''}tick(1/60)}run.enemies.length?null:Math.round(run.t*100)/100`);rows.push(row)}console.log(JSON.stringify(rows,null,2));
