import { sqliteTable, text, integer, uniqueIndex, index } from 'drizzle-orm/sqlite-core';
export const players=sqliteTable('players_v3',{id:text('id').primaryKey(),state:text('state').notNull(),revision:integer('revision').notNull().default(0),lastOp:text('last_op'),recoveryHash:text('recovery_hash'),createdAt:integer('created_at').notNull()},t=>[uniqueIndex('player_recovery').on(t.recoveryHash)]);
export const sessions=sqliteTable('sessions_v3',{hash:text('hash').primaryKey(),playerId:text('player_id').notNull(),expires:integer('expires').notNull()});
export const operations=sqliteTable('operations_v3',{playerId:text('player_id').notNull(),id:text('id').notNull(),fingerprint:text('fingerprint').notNull(),result:text('result').notNull(),createdAt:integer('created_at').notNull()},t=>[uniqueIndex('operation_identity').on(t.playerId,t.id)]);
export const rankings=sqliteTable('rankings_v3',{runId:text('run_id').primaryKey(),playerId:text('player_id').notNull(),difficulty:integer('difficulty').notNull(),stage:integer('stage').notNull(),weapon:text('weapon').notNull(),nickname:text('nickname').notNull(),rank:integer('rank').notNull(),weapons:text('weapons').notNull(),elapsedMs:integer('elapsed_ms').notNull(),kills:integer('kills').notNull(),createdAt:integer('created_at').notNull()},t=>[uniqueIndex('ranking_best').on(t.playerId,t.difficulty,t.stage,t.weapon),index('ranking_lookup').on(t.difficulty,t.stage,t.elapsedMs,t.kills)]);
export const recoveryLimits=sqliteTable('recovery_limits_v3',{key:text('key').primaryKey(),count:integer('count').notNull(),expires:integer('expires').notNull()});
